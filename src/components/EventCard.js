import React from 'react'
import {
    View,
    StyleSheet
} from 'react-native'

const EventCard = (props)=>{
    return(
        <View>

        </View>
    )
}

const styles = StyleSheet.create({
    
})

export default EventCard